# SlateDB S3 Configuration
# Uncomment and set these variables to use S3 instead of local storage

# Required for S3:
# SLATE_S3_BUCKET=your-bucket-name

# Optional S3 settings:
# SLATE_S3_PREFIX=slate-memory  # Default: slate-memory

# AWS credentials (FastMCP Cloud will handle these automatically)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1  # Default: us-east-1

# Note: If AWS credentials are not set here, they will be loaded from:
# 1. IAM role (in EC2/ECS/Lambda)
# 2. ~/.aws/credentials file
# 3. Environment variables